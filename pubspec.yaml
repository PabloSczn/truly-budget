name: truly_budget
description: A clean Flutter budget app — TrulyBudget
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  fl_chart: ^0.67.0
  url_launcher: ^6.2.6

dev_dependencies:
  flutter_lints: ^3.0.1 

flutter:
  uses-material-design: true
